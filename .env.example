# Solidrop — Environment Variables
# Copy this file to .env and adjust values as needed.
#   cp .env.example .env

# === Required ===
# S3 bucket name (production: nafell-solidrop-storage)
S3_BUCKET=solidrop-dev

# API authentication key
API_KEY=dev-api-key

# AWS credentials (for production use real IAM credentials)
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_REGION=ap-northeast-1

# === Local Development (MinIO) ===
# Uncomment these for local development with docker compose.
# Leave commented out for production (uses AWS S3 directly).

# Internal S3 endpoint (Docker service name)
S3_ENDPOINT_URL=http://minio:9000

# MinIO requires path-style addressing
S3_FORCE_PATH_STYLE=true

# Public endpoint for presigned URLs (accessible from host machine)
S3_PUBLIC_ENDPOINT_URL=http://localhost:9000
